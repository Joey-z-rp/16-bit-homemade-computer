class Main {
    function void main() {
        var int result;
        let result = Main.fib(3);
        do Memory.poke(4095, result);
        while (true) {
            do Memory.peek(4095);
        }
        return;
    }

    function int fib(int n) {
        if ((n < 1) | (n = 1)) {
            return n;
        }

        return Main.fib(n - 1) + Main.fib(n - 2);
    }
}
